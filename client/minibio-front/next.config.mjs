/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ['http://app.minibio.local:3000', 'http://app.minibio.local:3000', 'http://juan.minibio.local:3000', 'juan.minibio.local'],
};

export default nextConfig;
